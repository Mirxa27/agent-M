{
    "version": 2,
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/server/$1.js"
        },
        {
            "src": "/(.*)",
            "dest": "/client/dist/$1"
        }
    ]
}
