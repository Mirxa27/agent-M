{
  "tasks": {
    "test": "echo \"No test scripts defined in the package.json file.\"",
    "build": "npm install && npm run build",
    "launch": "npm install && npm run db:push && npm run dev"
  }
}